# ParcelPilot Environment Variables
# Copy to .env and provide your API key for Live Gemini Agent Mode

# Optional: Set for Live Tool-Calling Agent Mode.
# If omitted, ParcelPilot runs in Offline Deterministic Test Engine mode.
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash
